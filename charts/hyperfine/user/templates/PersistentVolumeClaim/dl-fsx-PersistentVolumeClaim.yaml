apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.fsx.pvcName }}
  namespace: {{ .Values.name }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: {{ .Values.fsx.volumeName }}
  resources:
    requests:
      storage: {{ .Values.fsx.storageSize }}